# If visitor has 'nf_jwt' with specific roles set, let them in.
/* /:splat 200! Role=admin,editor

# Else, redirect them to login portal site.
/* https://chipie.netlify.com/?site=https://chipie-gated.netlify.com/:splat 302!